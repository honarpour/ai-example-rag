# Optional: lets you run `python3 deploy.py` with no flags. Both can still be
# overridden with --gen-model-id / --api-key if passed on the command line.
GEN_MODEL_ID=global.anthropic.claude-haiku-4-5-20251001-v1:0

# Leave this commented out on your first deploy - deploy.py generates and prints
# a random key for you. Only uncomment/set this if you want to pin or rotate it
# to a specific value on a later deploy.
# API_KEY=
